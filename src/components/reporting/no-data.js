export default function NoData({ children }) {
  return (
    <div className="border border-[#d9d9d9] w-full min-h-[300px] text-center flex flex-col justify-center items-center">
      {children}
    </div>
  );
}
